services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - lego-data:/app/data
    env_file:
      - path: .env
        required: false
    restart: unless-stopped

volumes:
  lego-data:
